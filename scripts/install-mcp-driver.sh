#!/usr/bin/env bash
# DEPRECATED — use gen-mcp-driver-config.sh instead
#
# This old script wrote directly to QwenPaw's runtime directory
# (drivers/mcp/). Per user instructions, the new script only prints
# the config to console and saves it locally — the user manually
# installs it into QwenPaw.

echo "DEPRECATED: use ./scripts/gen-mcp-driver-config.sh instead"
echo "This script no longer writes to QwenPaw's runtime directory."
exit 1
