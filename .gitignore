# Secrets — NEVER commit (contents ignored, keep docs + placeholder)
secrets/*
!secrets/.gitkeep
!secrets/README.md
client_secret.json
token.json
*.key
*.pem

# Deploy scripts / automation JSON — contain host secrets, never commit
deploy-fix.sh
commit-and-toggle.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Environment
.env
.env.local
.env.*.local

# Session-local todo files
*_TODO.md

# Generated driver configs
drivers/mcp/google-workspace-mcp-generated.yaml

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# QwenPaw memory/journal notes (written to project dir, not project code)
memory/
